import s3_manager
import steps


def index_with_google_drive():
    """
    todo: sayandeep
    last_execution_time = mongo.get()
	for file in google_drive_after(last_execution_time):
		steps.save_file_to_internal_storage(path, file)
	mongo.set(last_execution_time, now())
    """
